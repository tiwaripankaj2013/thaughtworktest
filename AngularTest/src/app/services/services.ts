import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';

@Injectable()
// Service class for ajax to get Customer data from server
export class StudentApiService {

    // studentAPI = 'https://student-management-api-1u3cd4j7s.now.sh/students';

    // constructor(private httpclient: HttpClient) { }

    // getCustomerApi(): Observable<any> {
    //     return this.httpclient.get(this.studentAPI)
    // }

    // getCustomerDetailApi(rollNumber): Observable<any> {
    //     this.studentAPI=this.studentAPI+'/'+rollNumber;
    //     return this.httpclient.get(this.studentAPI);
        
    // }
}